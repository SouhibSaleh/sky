export interface PendingRequest {
    firstName: string,
    lastName: string,
    submitedOn: string,
    duration: number,
    firstDate: string,
    lastDate: string
    currentlyAt: string,
    imgUrl:string
}